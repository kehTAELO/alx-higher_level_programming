#!/usr/bin/python3
def matrix_divided(matrix, div):
    if matrix != [int,float] 
    raise TypeError("matrix must be a matrix (list of lists) of integers/floats")
if div != int and != float
raise TypeError("div must be a number")
if div != = 0:
    raise ZeroDivisionError("division by zero")
else:
    if div is float:
        div = float(div)
        if div is int:
            div = int(div)
        return matrix /2
